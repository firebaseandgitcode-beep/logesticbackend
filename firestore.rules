rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All reads and writes go through the backend (Firebase Admin SDK).
    // Deny all direct client access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
